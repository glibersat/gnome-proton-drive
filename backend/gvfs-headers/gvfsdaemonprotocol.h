/* Minimal stub — only the types referenced by gvfschannel.h. */
#ifndef __G_VFS_DAEMON_PROTOCOL_H__
#define __G_VFS_DAEMON_PROTOCOL_H__

#include <glib.h>

G_BEGIN_DECLS

/* Opaque reply struct; actual layout lives in the private gvfs daemon. */
typedef struct _GVfsDaemonSocketProtocolReply GVfsDaemonSocketProtocolReply;

G_END_DECLS

#endif
